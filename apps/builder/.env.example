# Sociarena Builder - Configuration
# Copier ce fichier vers .env et remplir les valeurs
# NE JAMAIS commiter .env dans git

# ===========================================
# DOCKER / POSTGRESQL
# Utilisé par docker-compose.yml
# ===========================================
POSTGRES_USER=webstudio
POSTGRES_PASSWORD=  # Générer avec: openssl rand -base64 24
POSTGRES_DB=webstudio

# JWT pour PostgREST (min 64 caractères)
# Générer avec: openssl rand -base64 48
PGRST_JWT_SECRET=

# ===========================================
# BASE DE DONNÉES (Application)
# ===========================================
DATABASE_URL=postgresql://webstudio:YOUR_PASSWORD@webstudio-postgres:5432/webstudio
DIRECT_URL=postgresql://webstudio:YOUR_PASSWORD@webstudio-postgres:5432/webstudio

# ===========================================
# SECRETS D'AUTHENTIFICATION
# Générer avec: openssl rand -hex 32
# ===========================================
AUTH_SECRET=
SESSION_SECRET=
COOKIE_SECRET=
ENCRYPTION_SECRET=

# ===========================================
# OIDC (Infomaniak)
# ===========================================
OIDC_ISSUER=https://login.infomaniak.com
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret
OIDC_REDIRECT_URI=https://your-domain.com/auth/infomaniak/callback
OIDC_SCOPES='openid profile email'

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV=production
DEV_LOGIN=false
PUBLIC_URL=https://your-domain.com/

# PostgREST
POSTGREST_URL=http://webstudio-postgrest:3000
POSTGREST_API_KEY=  # Même valeur que PGRST_JWT_SECRET encodée en JWT

# Features
FEATURES=*
USER_PLAN=pro
MAX_ASSETS_PER_PROJECT=50

# ===========================================
# S3/R2 STORAGE (Optionnel)
# ===========================================
# S3_BUCKET=
# S3_REGION=
# S3_ENDPOINT=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=

# ===========================================
# CORS
# ===========================================
ALLOWED_ORIGINS=https://your-domain.com/
